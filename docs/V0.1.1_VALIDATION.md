# v0.1.1 验证重点

金马 44 重新导入后应确认：

1. `event.name` 为 `台北金马影展`。
2. JSON 直接显示中文，不再主要表现为 `\uXXXX`。
3. `relatedEditions` 不再只有 1 条，应包含完整名单页底部可见的历届年份。
4. `relatedEditions[].url` 统一为 `/nominees?k=a`。
5. 25 个奖项类别和既有 Subject/Person/Image 关系不应因本次修复改变。
6. 再导入同一届，SQLite 不应产生重复 Edition/Entry。

随后再用另一个完全不同的豆瓣奖项做跨奖项验证。
