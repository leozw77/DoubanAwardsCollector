# v0.1.2 验证重点

重新导入：

`https://movie.douban.com/awards/venice/64/`

重点确认：

1. `event.name` 仍为 `威尼斯电影节`。
2. 新结果中必须出现 `金狮奖 Golden Lion`。
3. 该类别应由 `groupName == name == "金狮奖 Golden Lion"` 表示。
4. 金狮奖条目应正常保留 Winner / Nominee、Subject、Person、Image。
5. v0.1.1 已有类别不应消失或改变。
6. `AwardEditionData` 仍为 `schemaVersion: 1`，ParserVersion 应为 `1.0.2`。
7. 再导入一次同一届，不应产生重复数据库记录。

如果威尼斯 64 通过，再抽测一个结构不同的奖项/届次；若无新结构性漏采，可冻结 AwardEditionData v1。
